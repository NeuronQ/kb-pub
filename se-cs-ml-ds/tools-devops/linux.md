## Find out version

```sh
cat /etc/*-release
lsb_release -a
cat /proc/version
hostnamectl
```

## Install packages

### Debians

#### Mid-level (recommended) - apt

```
sudo apt update
sudo apt install redis-server -y
```
